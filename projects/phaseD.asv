%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
t= 0:0.0001:3;
Phase = 0;
s = sin(2*pi*587*t + Phase);
plot(t,s);
grid on;
xlabel('seconds');
ylabel('s(t)');
title('Note D with phase');
xlim([0 0.01]);
ylim([-1.5 1.5]);
soundsc(s);
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%